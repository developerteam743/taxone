import {describe,it,expect} from 'vitest'; import Decimal from 'decimal.js'; import {assertBalanced,accountBalance} from './index';
describe('accounting',()=>{it('rejects unbalanced journals',()=>expect(()=>assertBalanced([{accountId:'a',debit:100,credit:0},{accountId:'b',debit:0,credit:90}])).toThrow());it('balances decimal entries',()=>expect(()=>assertBalanced([{accountId:'a',debit:'100.10',credit:0},{accountId:'b',debit:0,credit:'100.10'}])).not.toThrow());it('computes asset balance',()=>expect(accountBalance(new Decimal('125.50'),10,'ASSET').toFixed(2)).toBe('115.50'));});
