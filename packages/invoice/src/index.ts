import Decimal from 'decimal.js'; export function lineTotal(quantity:Decimal.Value,rate:Decimal.Value,discount:Decimal.Value=0){return new Decimal(quantity).mul(rate).minus(discount)} export function invoiceTotal(taxable:Decimal.Value,cgst:Decimal.Value,sgst:Decimal.Value,igst:Decimal.Value,cess:Decimal.Value){return [taxable,cgst,sgst,igst,cess].reduce((s,v)=>s.plus(v),new Decimal(0))}
