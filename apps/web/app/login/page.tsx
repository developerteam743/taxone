'use client'; import {useState} from 'react';
export default function Login(){const[email,setEmail]=useState('admin@example.com');const[pw,setPw]=useState('Admin@12345');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('Signing in…');try{const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000'}/api/v1/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password:pw})});const d=await r.json();if(!r.ok)throw new Error(d.message??'Login failed');localStorage.setItem('taxone_token',d.accessToken);location.href='/';}catch(e){setMsg(e instanceof Error?e.message:'Login failed')}}return <main className="min-h-screen grid place-items-center p-5"><form onSubmit={submit} className="card w-full max-w-md"><h1 className="text-3xl font-bold mb-2">TaxOne</h1><p className="muted mb-7">Sign in to your accounting workspace</p><label className="block text-sm font-medium mb-2">Email</label><input className="input mb-4" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/><label className="block text-sm font-medium mb-2">Password</label><input className="input mb-5" value={pw} onChange={e=>setPw(e.target.value)} type="password" required/><button className="btn bg-slate-900 text-white w-full">Sign in</button>{msg&&<p className="text-sm mt-4">{msg}</p>}</form></main>}
