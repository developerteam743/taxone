# TAXONE — START / RESUME

Repository: `developerteam743/taxone`

**Start now.**

1. Inspect the current GitHub repository and latest commit.
2. Read `00_MASTER_PROJECT_BLUEPRINT.md` and the other existing project specifications.
3. Check what has already been implemented.
4. Identify exactly where the previous session stopped.
5. Continue from that point.
6. Do not recreate, overwrite, or delete existing work unnecessarily.
7. Follow the repository specifications and production/security requirements.
8. Implement the next logical task — do not only give me a plan.
9. Test and verify your changes.
10. At the end, report:

* Completed
* Changed
* Verified
* Remaining
* Next task

**Important:** GitHub repository state is the source of truth. Never assume what the previous AI session completed. Inspect first, then continue.

**DO NOT START OVER. CONTINUE FROM WHERE THE PROJECT ACTUALLY IS.**
